create table demographics (
    subject_id text primary key,
    age integer,
    sex text,
    race text,
    primary_diagnosis text,
    ecog_status integer,
    consent_date date,
    enrollment_date date

);
